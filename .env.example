# Copy to .env and edit

# PV prefix
P=TC400:

# Serial-to-Ethernet converter wired to the TC 400 RS-485 port
DEVICE_IP=192.168.0.10
DEVICE_PORT=4001

# RS-485 address of the drive unit (P797), 3 digits
ADDR=001
